5750d867ef12f2ecb014dbbd91a5f972


var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

var symToStringTag = require('./_Symbol') ? typeof require('./_Symbol') === "function" ? require('./_Symbol').toStringTag : "@@toStringTag" : undefined;

function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return symToStringTag && symToStringTag in Object(value) ? require('./_getRawTag')(value) : require('./_objectToString')(value);
}

module.exports = baseGetTag;